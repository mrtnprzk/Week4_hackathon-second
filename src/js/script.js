import { Feature } from "./feature.js";
import { gettingData } from "./fetching.js";

//waiting for the DOM to load
document.addEventListener("DOMContentLoaded", () => {
  //initial creation of elements
  gettingData();
});
